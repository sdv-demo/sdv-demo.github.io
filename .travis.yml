branches:
  only:
    - master

language: ruby

rvm:
  - 2.7.1

git:
  depth: false

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $OAUTH_TOKEN
  keep-history: true
  verbose: true
  target-branch: master
  on:
    branch: master
    condition: $TRAVIS_EVENT_TYPE = "cron"
